{
  "name": "sample_nodejs_app",
  "version": "1.0.0",
  "description": "A sample application",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test\" && exit 1"
  },
  "keywords": [
    "aws",
    "ecs",
    "fargate",
    "ecr"
  ],
  "dependencies": {
    "express": "^4.17.1"
  }
}
